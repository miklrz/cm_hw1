# Имя пользователя, отображаемое в приглашении
user = "test_user"

# Имя компьютера, отображаемое в приглашении
hostname = "test_localhost"

# Путь к архиву виртуальной файловой системы (tar-файл)
filesystem_path = "test_fs.tar"

# Путь к файлу лога (json-файл)
log_path = "test_log.json"

# Путь к стартовому скрипту (текстовый файл с командами, по одной на строку)
startup_script = "test_startup_script.txt"
